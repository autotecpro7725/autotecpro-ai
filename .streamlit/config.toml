[client]
# Hide Streamlit developer tools, including Clear cache.
toolbarMode = "viewer"

[server]
# Keep this aligned with MAX_UPLOAD_SIZE_MB in App/app.py.
maxUploadSize = 20
maxMessageSize = 50
enableWebsocketCompression = true
